#include <random>
#include <algorithm>
#include "MemcachedClient.h"
#include "OurScheme.h"

int main() {{
    ConfigParameter gcp({0});
    OurScheme os({0});
    os.CorrelationAnalysis();
    return 0;
}}
